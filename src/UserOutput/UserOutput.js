import React, { useState } from 'react';

const userOutput = (props) => {
  return (
    <div>
      <p>{props.username}</p>
      <p>Text2</p>
    </div>
  );
}

export default userOutput;
